# 履歴表示修正と再発防止策導入の報告 (v57)

履歴表示の更なる改善と、プロジェクト運用の健全化を目的とした修正・追加を行いました。

## 変更内容

### 1. UI修正（履歴表示詳細モード）

- **チェックボックスの配置修正**:
  - これまで `position: absolute` で右端に固定していたため、機種名が長い場合に overlap（重なり）が発生していました。
  - 修正後は、ヘッダー内の `flex` 配置に組み込み、機種名の `h4` とチェックボックスが左右に正しく分かれるようにしました。

### 2. 知見蓄積・再発防止策の導入

- **[FAILURES_LOG.md](file:///h:/gravity/pachinko-ev-calculator/docs/FAILURES_LOG.md) の作成**:
  - 過去のリリー手順失念（4回）の記録と、自分（エージェント）への強制ルールを明文化しました。
  - 同時に「デザイン・UI変更の変遷」セクションを設け、過去にどのような修正が行われ、どのような教訓を得たかを記録し続ける体制を整えました。
- **[SKILL.md](file:///h:/gravity/pachinko-ev-calculator/.agent/skills/pachinko-calculator-maint/SKILL.md) への追記**:
  - 作業開始時に必ず `FAILURES_LOG.md` を確認するよう、警告を追加しました。

## リリース情報

- **バージョン**: `v57`
- **GitHub反映**: 完了（プッシュおよびタグ `v57` 付与）

## 検証結果

- 機種名が長い場合でも、チェックボックスとテキストが重ならないことを確認。
- 修正完了からリリースまで、ユーザーの指示を待たず自律的に完遂。
