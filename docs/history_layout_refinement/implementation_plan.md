# 履歴表示の再修正計画 (v66)

前回の修正で発生したレイアウトの不備を修正し、最新の要望を反映します。

## 変更内容

### 1. 詳細表示の修正

- **改行の解除**: `h4` 内の `flex-direction: column` を解除し、機種名と貸玉（円）が同じ行に表示されるようにします。
- **チェックボックスの位置**: `header-right` のレイアウトを見直し、違和感のない位置に調整します。
- **文字色の回帰**: 追加していたアクセントカラーを削除し、以前の配色に戻します。

### 2. 簡略表示の修正

- **機種・貸玉の統合**: 1行目に `機種名 (貸玉円)` の形式で表示するように変更します。
- **改行の抑制**: 貸玉の改行をなくし、スッキリとした1行目レイアウトにします。

### 3. リリース作業

- `app.js` を v66 へ更新。
- `FAILURES_LOG.md` への追記（知見の更新）。
- `overview.md` の更新履歴を更新。
- GitHub へのプッシュおよび Git タグ `v66` の付与。

## 検証計画

- 自律実行ルールに則り、各表示モードでのレイアウト崩れがないことを確認し、そのままリリースを完遂します。
