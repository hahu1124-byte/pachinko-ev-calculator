---
description: pachinko-ev-calculatorの更新とGitHubへのプッシュ
---
// turbo-all

1. `git add .` を実行して変更をステージングする
2. `git commit -m "アップデート内容"` の形式でコミットする
3. `git push` を実行して、`https://github.com/hahu1124-byte/pachinko-ev-calculator` リポジトリに変更を反映させる
